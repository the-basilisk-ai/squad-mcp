query DocumentList($limit: Int, $offset: Int) {
  documentList(limit: $limit, offset: $offset) {
    id
    displayId
    title
    kind
    path
    tags
    updatedAt
    briefStatus
    briefType
  }
}

query BriefList($filters: BriefListFilters, $limit: Int, $offset: Int) {
  briefList(filters: $filters, limit: $limit, offset: $offset) {
    id
    displayId
    title
    briefStatus
    briefType
    decisionRecommendation
    updatedAt
    sourceInsight {
      id
      displayId
      title
    }
    sourceAction {
      id
      displayId
      title
    }
  }
}

mutation CreateDocument($input: CreateDocumentInput!) {
  createDocument(input: $input) {
    id
    displayId
    title
    path
  }
}

mutation UpdateDocument($id: String!, $input: UpdateDocumentInput!) {
  updateDocument(id: $id, input: $input) {
    id
    displayId
    title
    updatedAt
  }
}

mutation AddDocumentTag($id: String!, $tag: String!) {
  addDocumentTag(id: $id, tag: $tag) {
    id
    tags
  }
}

mutation RemoveDocumentTag($id: String!, $tag: String!) {
  removeDocumentTag(id: $id, tag: $tag) {
    id
    tags
  }
}

mutation GenerateBriefFromAction($actionId: String!, $type: BriefType) {
  generateBriefFromAction(actionId: $actionId, type: $type) {
    briefId
    briefDisplayId
  }
}

mutation GenerateBriefFromInsight($insightId: String!, $type: BriefType) {
  generateBriefFromInsight(insightId: $insightId, type: $type) {
    id
    displayId
    title
    briefStatus
  }
}

mutation RetryBriefGeneration($briefId: String!) {
  retryBriefGeneration(briefId: $briefId) {
    id
    displayId
    briefStatus
  }
}

mutation SetBriefStatus($briefId: String!, $status: BriefStatus!) {
  setBriefStatus(briefId: $briefId, status: $status) {
    id
    displayId
    title
    briefStatus
  }
}
